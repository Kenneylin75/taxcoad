const { Pool } = require('pg'); const pool = new Pool({ connectionString: 'postgres://postgres:postgres@localhost:5432/temple_db' }); pool.query(\UPDATE distributors SET quota = 30 WHERE account = 'admin510'\, (err, res) => { if (err) console.error(err); else console.log('Updated admin510 quota to 30:', res.rowCount); pool.query(\UPDATE distributor_applications SET custom_nodes = 30 WHERE account = 'admin510'\, (err2, res2) => { if (err2) console.error(err2); else console.log('Updated admin510 application to 30:', res2.rowCount); process.exit(); }); });
