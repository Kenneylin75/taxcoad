const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const distId = 'dist-RX6X3WRY'; let list = await prisma.temple.findMany({ orderBy: { createdAt: 'desc' } }); const allSales2 = await prisma.distributorSales.findMany(); list = list.filter((t) => { if (t.distributorId !== distId) return false; const sales = allSales2.find((s) => s.id === t.salesId); if (sales && sales.role === 'SuperSales') return false; return true; }); console.log('Pending apps for dist:', list.filter(t => t.status === 'Pending').length); } main();
